query GetCountryByCode($code: ID!) {
    country(code: $code) {
        code
        name
        currency
        capital
        emoji
        languages {
            code
            name
            native
        }
        phone
        continent {
            code
            name
        }
    }
}